package la.scala.codejam.tic_tac_toe_tomek

import scala.util.matching.Regex.Match

object TicTacToeTomek {
  def main(args: Array[String]): Unit = {
    val cases = Integer.parseInt(readLine())
    for( i <- 0 until cases){
      //println( decode( readLine() ) )
    }
  }
}
